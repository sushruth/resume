<? import type { PublicationEntry } from '../../sync.types'; ?>

\skills {\href{<?= entry.url || '' ?>}{<?= entry.name || '' ?>}}
(<?= entry.publisher || '' ?>)
\hfill {<?= entry.releaseDate || '' ?>}
